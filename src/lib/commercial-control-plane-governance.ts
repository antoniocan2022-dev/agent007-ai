import {db} from './db'
import {COMMERCIAL_BUSINESSES,COMMERCIAL_CATEGORIES,COMMERCIAL_CONTROL_PLANE_ID,COMMERCIAL_CONTROL_PLANE_VERSION,type AuthorityLevel,type BillingRecord,type CommercialAuditRecord,type CommercialBusiness,type CommercialControlPlaneSnapshot,type CommercialEvidence,type DelegatedAuthority,type Entitlement} from './commercial-control-plane'
const now=()=>new Date().toISOString()
export async function recordBilling(input:Omit<BillingRecord,'billingId'|'createdAt'|'updatedAt'>&{billingId?:string}){if(!input.tenantId.trim()||!COMMERCIAL_BUSINESSES.includes(input.business))throw new Error('Valid tenantId and business are required.');const currency=input.currency.trim().toUpperCase();if(currency.length!==3)throw new Error('currency must be a three-letter code.');if(!input.product.trim()||!input.idempotencyKey.trim())throw new Error('product and idempotencyKey are required.');const billing:BillingRecord={...input,billingId:input.billingId?.trim()||`bill_${Date.now()}_${Math.random().toString(36).slice(2,8)}`,amount:Number.isFinite(input.amount)?Math.max(0,input.amount):0,currency,product:input.product.trim(),idempotencyKey:input.idempotencyKey.trim(),createdAt:now(),updatedAt:now()},key=`billing:${input.tenantId}:${billing.idempotencyKey}`,existing=await db.memory.findUnique({where:{key}});if(existing)return{created:false,billing:JSON.parse(existing.value) as BillingRecord};await db.memory.create({data:{key,category:COMMERCIAL_CATEGORIES.billing,value:JSON.stringify(billing)}});return{created:true,billing}}
export async function grantEntitlement(input:Omit<Entitlement,'entitlementId'|'createdAt'|'updatedAt'>&{entitlementId?:string}){if(!input.tenantId.trim()||!COMMERCIAL_BUSINESSES.includes(input.business)||!input.product.trim()||!input.subjectId.trim())throw new Error('Valid entitlement identity is required.');const entitlement:Entitlement={...input,entitlementId:input.entitlementId?.trim()||`ent_${Date.now()}_${Math.random().toString(36).slice(2,8)}`,product:input.product.trim(),subjectId:input.subjectId.trim(),limits:Object.fromEntries(Object.entries(input.limits??{}).map(([k,v])=>[k.trim(),Number.isFinite(v)?Math.max(0,v):0])),createdAt:now(),updatedAt:now()},key=`entitlement:${input.tenantId}:${entitlement.product}:${entitlement.subjectId}`,existing=await db.memory.findUnique({where:{key}});if(existing)return{created:false,entitlement:JSON.parse(existing.value) as Entitlement};await db.memory.create({data:{key,category:COMMERCIAL_CATEGORIES.entitlement,value:JSON.stringify(entitlement)}});return{created:true,entitlement}}
export async function recordCommercialEvidence(input:Omit<CommercialEvidence,'evidenceId'|'createdAt'>&{evidenceId?:string}){if(!input.tenantId.trim()||!COMMERCIAL_BUSINESSES.includes(input.business)||!input.source.trim()||!input.statement.trim())throw new Error('Valid evidence identity is required.');const evidence:CommercialEvidence={...input,evidenceId:input.evidenceId?.trim()||`ce_${Date.now()}_${Math.random().toString(36).slice(2,8)}`,source:input.source.trim(),statement:input.statement.trim(),confidence:Number.isFinite(input.confidence)?Math.max(0,Math.min(1,input.confidence)):0,createdAt:now()};await db.memory.create({data:{key:`evidence:${input.tenantId}:${evidence.evidenceId}`,category:COMMERCIAL_CATEGORIES.evidence,value:JSON.stringify(evidence)}});return evidence}
export async function grantDelegatedAuthority(input:Omit<DelegatedAuthority,'authorityId'>&{authorityId?:string}){if(!input.tenantId.trim()||!COMMERCIAL_BUSINESSES.includes(input.business)||!input.action.trim()||!input.approvedByUserId.trim())throw new Error('Valid authority identity is required.');const authority:DelegatedAuthority={...input,authorityId:input.authorityId?.trim()||`auth_${Date.now()}_${Math.random().toString(36).slice(2,8)}`,action:input.action.trim(),maxSpend:input.maxSpend===null?null:Math.max(0,input.maxSpend),maxDailyCount:input.maxDailyCount===null?null:Math.max(0,Math.floor(input.maxDailyCount)),allowedChannels:[...new Set((input.allowedChannels??[]).map(x=>x.trim()).filter(Boolean))]},key=`authority:${input.tenantId}:${authority.authorityId}`,existing=await db.memory.findUnique({where:{key}});if(existing)return JSON.parse(existing.value) as DelegatedAuthority;await db.memory.create({data:{key,category:COMMERCIAL_CATEGORIES.authority,value:JSON.stringify(authority)}});return authority}
export async function evaluateDelegatedAuthority(input:{tenantId:string;business:CommercialBusiness;action:string;spend?:number;channel?:string;dailyCount?:number}){const rows=await db.memory.findMany({where:{category:COMMERCIAL_CATEGORIES.authority}}),t=Date.now(),matches=rows.map(r=>{try{return JSON.parse(r.value) as DelegatedAuthority}catch{return null}}).filter((a):a is DelegatedAuthority=>!!a&&a.tenantId===input.tenantId&&a.business===input.business&&a.action===input.action.trim()&&a.status==='active'&&(!a.expiresAt||new Date(a.expiresAt).getTime()>t)),authority=matches.find(a=>a.level==='autonomous'||a.level==='guardrailed')??matches[0]??null;if(!authority)return{allowed:false,authority:null,reason:'No active delegated authority matches this action.'};if(authority.level==='human_approval'||authority.level==='forbidden')return{allowed:false,authority,reason:`Authority level ${authority.level} does not permit autonomous execution.`};if(authority.maxSpend!==null&&Math.max(0,input.spend??0)>authority.maxSpend)return{allowed:false,authority,reason:'Requested spend exceeds delegated authority.'};if(authority.maxDailyCount!==null&&Math.max(0,Math.floor(input.dailyCount??0))>authority.maxDailyCount)return{allowed:false,authority,reason:'Daily action count exceeds delegated authority.'};if(authority.allowedChannels.length>0&&input.channel&&!authority.allowedChannels.includes(input.channel))return{allowed:false,authority,reason:'Requested channel is outside delegated authority.'};return{allowed:true,authority,reason:'Action is permitted within active delegated authority.'}}
export async function auditCommercialAction(input:Omit<CommercialAuditRecord,'auditId'|'createdAt'>&{auditId?:string}){const audit:CommercialAuditRecord={...input,auditId:input.auditId?.trim()||`audit_${Date.now()}_${Math.random().toString(36).slice(2,8)}`,createdAt:now()};await db.memory.create({data:{key:`audit:${input.tenantId}:${audit.auditId}`,category:COMMERCIAL_CATEGORIES.audit,value:JSON.stringify(audit)}});return audit}
export async function getCommercialControlPlaneSnapshot(tenantId?:string):Promise<CommercialControlPlaneSnapshot>{const groups=await Promise.all(Object.values(COMMERCIAL_CATEGORIES).map(async category=>({category,rows:await db.memory.findMany({where:{category},take:5000})}))),parsed=new Map<string,Record<string,unknown>[]>();for(const group of groups)parsed.set(group.category,group.rows.map(r=>{try{return JSON.parse(r.value) as Record<string,unknown>}catch{return null}}).filter((v):v is Record<string,unknown>=>!!v));const scoped=(category:string)=>(parsed.get(category)??[]).filter(v=>!tenantId||v.tenantId===tenantId),count=(category:string,business:CommercialBusiness)=>scoped(category).filter(v=>v.business===business).length,businesses=Object.fromEntries(COMMERCIAL_BUSINESSES.map(b=>[b,{customers:count(COMMERCIAL_CATEGORIES.customer,b),events:count(COMMERCIAL_CATEGORIES.event,b),workflows:count(COMMERCIAL_CATEGORIES.workflow,b),billing:count(COMMERCIAL_CATEGORIES.billing,b),evidence:count(COMMERCIAL_CATEGORIES.evidence,b)}])) as CommercialControlPlaneSnapshot['businesses'],issues:string[]=[];const events=scoped(COMMERCIAL_CATEGORIES.event),workflows=scoped(COMMERCIAL_CATEGORIES.workflow);if(new Set(events.map(v=>v.idempotencyKey)).size!==events.length)issues.push('Duplicate event idempotency keys detected.');if(new Set(workflows.map(v=>v.idempotencyKey)).size!==workflows.length)issues.push('Duplicate workflow idempotency keys detected.');for(const v of scoped(COMMERCIAL_CATEGORIES.credential))if(typeof v.secretRef==='string'&&/(^sk_|^rk_|^pk_|password=|token=)/i.test(v.secretRef))issues.push(`Credential ${String(v.credentialId)} appears to contain raw secret material.`);return{id:COMMERCIAL_CONTROL_PLANE_ID,version:COMMERCIAL_CONTROL_PLANE_VERSION,tenantCount:scoped(COMMERCIAL_CATEGORIES.tenant).length,customerCount:scoped(COMMERCIAL_CATEGORIES.customer).length,eventCount:events.length,workflowCount:workflows.length,credentialCount:scoped(COMMERCIAL_CATEGORIES.credential).length,billingCount:scoped(COMMERCIAL_CATEGORIES.billing).length,entitlementCount:scoped(COMMERCIAL_CATEGORIES.entitlement).length,evidenceCount:scoped(COMMERCIAL_CATEGORIES.evidence).length,activeAuthorityCount:scoped(COMMERCIAL_CATEGORIES.authority).filter(v=>v.status==='active').length,auditCount:scoped(COMMERCIAL_CATEGORIES.audit).length,businesses,integrity:{ok:issues.length===0,issues}}}
export function validateCommercialControlPlaneContracts(){const errors:string[]=[];if(COMMERCIAL_BUSINESSES.length!==4)errors.push('Commercial taxonomy must contain three venture units plus shared-platform.');if(Object.values(COMMERCIAL_CATEGORIES).length!==15)errors.push('Commercial persistence taxonomy must contain 15 categories.');if(new Set(Object.values(COMMERCIAL_CATEGORIES)).size!==Object.values(COMMERCIAL_CATEGORIES).length)errors.push('Commercial persistence categories must be unique.');if(!['autonomous','guardrailed','human_approval','forbidden'].every(v=>(['autonomous','guardrailed','human_approval','forbidden'] as AuthorityLevel[]).includes(v as AuthorityLevel)))errors.push('Authority taxonomy is invalid.');return errors}
