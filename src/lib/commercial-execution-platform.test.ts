import { describe, expect, it } from 'bun:test'
import { COMMERCIAL_CATEGORIES } from './commercial-control-plane'
import { PHASE6_TOOL_IDS, createWebhookSignature, verifyWebhookSignature, registerCommercialProvider, getCommercialProvider, registerCommercialProviderAdapter, listCommercialProviderAdapters } from './commercial-execution-platform'
describe('Phase 6',()=>{it('defines nine execution capabilities',()=>{expect(PHASE6_TOOL_IDS).toHaveLength(9);expect(new Set(PHASE6_TOOL_IDS).size).toBe(9)});it('extends the commercial taxonomy safely',()=>{const v=Object.values(COMMERCIAL_CATEGORIES);expect(new Set(v).size).toBe(v.length);expect(v.length).toBe(15)});it('verifies signed webhooks',()=>{const body='{"id":"evt_1"}',secret='phase6-test-secret',t=1800000000,s=createWebhookSignature(body,secret,t);expect(verifyWebhookSignature(body,s,secret,t)).toBe(true);expect(verifyWebhookSignature(body,s,'wrong-secret',t)).toBe(false);expect(verifyWebhookSignature(body,s,secret,t+301)).toBe(false)});it('persists provider definitions',async()=>{const id='phase6-test-'+Date.now();const p=await registerCommercialProvider({providerId:id,name:'Phase6Test',version:'1.0.0',capabilities:['read'],businesses:['revenue-recovery'],environments:['sandbox'],webhookEvents:[],status:'enabled'});expect(p.providerId).toBe(id);expect((await getCommercialProvider(id))?.version).toBe('1.0.0')});it('keeps adapters inspectable',()=>{const id='phase6-adapter-'+Date.now();registerCommercialProviderAdapter({providerId:id,execute:async()=>({ok:true,observedAt:new Date().toISOString(),latencyMs:1})});expect(listCommercialProviderAdapters()).toContain(id)})})
