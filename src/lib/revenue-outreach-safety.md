# Outreach Safety

Outbound outreach is an external side effect. Agent007 must never infer authorization from the existence of an opportunity, customer, or generated message alone.

Execution requires: explicit approval, an exact registered executor, explicit provider configuration, validated recipient/content, and durable action identity.

A successful SMTP submission is an execution event only. Financial truth remains processor-backed transaction evidence.