import { createCommercialWorkflow, recordCommercialEvent } from './commercial-control-plane'
import { auditCommercialAction, evaluateDelegatedAuthority } from './commercial-control-plane-governance'
import type { RecoveryOutcome } from './revenue-recovery-contract'
export async function prepareRecoveryWork(input:{tenantId:string;customerId:string;opportunityId:string;action:string;spend?:number;channel?:string}){const authority=await evaluateDelegatedAuthority({tenantId:input.tenantId,business:'revenue-recovery',action:input.action,spend:input.spend,channel:input.channel});await auditCommercialAction({tenantId:input.tenantId,business:'revenue-recovery',action:input.action,actor:'REVENUE_RECOVERY_LEADER',entityType:'recovery_opportunity',entityId:input.opportunityId,allowed:authority.allowed,reason:authority.reason,metadata:{customerId:input.customerId,channel:input.channel??null,spend:input.spend??0}});if(!authority.allowed)return{queued:false,workflowId:null,reason:authority.reason};const event=await recordCommercialEvent({tenantId:input.tenantId,business:'revenue-recovery',type:'recovery.work.requested',source:'revenue-recovery',entityType:'recovery_opportunity',entityId:input.opportunityId,occurredAt:new Date().toISOString(),payload:{customerId:input.customerId,action:input.action,channel:input.channel??null,spend:input.spend??0},idempotencyKey:`recovery-work:${input.opportunityId}:${input.action}`});const workflow=await createCommercialWorkflow({tenantId:input.tenantId,business:'revenue-recovery',workflowType:'recovery-work',input:{customerId:input.customerId,opportunityId:input.opportunityId,action:input.action,channel:input.channel??null,spend:input.spend??0,eventId:event.event.eventId},maxRetries:3,nextRunAt:new Date().toISOString(),idempotencyKey:`recovery-workflow:${input.opportunityId}:${input.action}`});return{queued:workflow.created,workflowId:workflow.workflow.workflowId,reason:workflow.created?'queued':'already queued'}}
export async function recordRecoveryOutcome(input:Omit<RecoveryOutcome,'outcomeId'|'recoveryRate'|'createdAt'>&{outcomeId?:string}){if(!input.tenantId.trim()||!input.customerId.trim()||!input.opportunityId.trim())throw new Error('tenantId, customerId, and opportunityId are required.');const expected=Math.max(0,Number.isFinite(input.expectedRevenue)?input.expectedRevenue:0),recovered=Math.min(expected,Math.max(0,Number.isFinite(input.recoveredRevenue)?input.recoveredRevenue:0));const outcome:RecoveryOutcome={...input,outcomeId:input.outcomeId?.trim()||`rro_${input.opportunityId}`,expectedRevenue:expected,recoveredRevenue:recovered,recoveryRate:expected>0?Math.round(recovered/expected*10000)/10000:0,observedSource:input.observedSource.trim(),createdAt:new Date().toISOString()};const{db}=await import('./db');const key=`revenue_recovery_outcome:${input.tenantId}:${input.customerId}:${input.opportunityId}`;const existing=await db.memory.findUnique({where:{key}});if(existing)return{created:false,outcome:JSON.parse(existing.value) as RecoveryOutcome};await db.memory.create({data:{key,category:'revenue_recovery_outcome',value:JSON.stringify(outcome)}});return{created:true,outcome}}
