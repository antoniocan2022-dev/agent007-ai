name: _fix-p5-list-header
on:
  push:
    branches: [feat/ceo-p1-p4-conversational-integrity]
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: feat/ceo-p1-p4-conversational-integrity
          fetch-depth: 0
      - name: Fix exact list-header grammar and remove patcher
        env:
          BRANCH: feat/ceo-p1-p4-conversational-integrity
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('src/lib/ceo-reference-resolution.ts')
          s = p.read_text()
          old = r"const LIST_HEADER_RE = /^(?:options?|alternatives?|choices?|priorities?|steps?|approaches?|plans?|items?)\s*:?\s*$/i"
          new = r"const LIST_HEADER_RE = /^(?:(?:new|alternative|other)\s+)?(?:options?|alternatives?|choices?|priorities?|steps?|approaches?|plans?|items?)\s*:?\s*$/i"
          if old not in s:
              raise SystemExit('LIST_HEADER_RE anchor not found')
          p.write_text(s.replace(old, new))
          PY
          git rm -f .github/workflows/_fix_p5_list_header.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(ceo): recognize alternative list headers"
          git push origin "$BRANCH"
