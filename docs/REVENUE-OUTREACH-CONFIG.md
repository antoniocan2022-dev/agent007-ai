# Outreach Configuration

The SMTP outreach executor is fail-closed and disabled unless explicitly enabled. Provider credentials must be supplied through deployment environment variables; no credentials belong in source control.